# ===========================================================================
# Backend Environment Template (Python ML Microservice)
# ===========================================================================

# 1. Allowed Frontend Origin for CORS
# In local development: http://localhost:3000
# In production: https://your-app.vercel.app (or custom domain)
FRONTEND_URL=http://localhost:3000

# 2. Server Port (Default: 8008)
PORT=8008

# Note: Database persistence, Active Learning sync, and User Auth
# are now handled directly by Supabase on the frontend.
